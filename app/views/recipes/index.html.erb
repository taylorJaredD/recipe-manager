<h2>Recipes <a href="/recipes/new">(+)</a></h2>

<!-- <ul>
  <% @recipes.each do |recipe| %>
    <li>
      <a href="/recipes/<%= recipe.id %>">
        <%= recipe.title %>
      </a>
    </li>
  <% end %>
</ul> -->
<div class="recipe-box">
<% @recipes.each do |recipe| %>

  <div class="recipe">
    <h2><%= link_to recipe.title, recipe_path(recipe) %></h2>
    <img class="img" src="<%= recipe.img_url %>">
  </div>
<% end %>

</div>
